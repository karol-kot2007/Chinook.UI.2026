<UserControl x:Class="Chinook.UI.AlbumInfoControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Chinook.UI"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

  <Grid Background="#FF434344">
    <Grid.RowDefinitions>
      <RowDefinition Height="auto"/>
      <RowDefinition Height="auto"/>
      <RowDefinition  Height="*"/>
    </Grid.RowDefinitions>


    <local:AlbumSwapper  x:Name="ArtistSwapper" HorizontalAlignment="Center" Grid.Row="0"  Height="30" 
     Margin="10,15,10,0"/>

    <local:AlbumSwapper x:Name="AlbumSwapper" HorizontalAlignment="Center"  Grid.Row="1" Height="30" 
     Margin="10,15,10,0" Loaded="AlbumSwapper_Loaded"/>

    <DataGrid VerticalAlignment="top" 
          HorizontalAlignment="Stretch" Margin="10 30 10 0" x:Name="GridAlbum"  Background="#FF434344" Grid.Row="2"
  RowBackground="#FF434344" Foreground="#FF434344" AutoGenerateColumns="False" >
      <DataGrid.Columns  >
        <DataGridTextColumn  Width="*"  Header="Composer:" Binding="{Binding Composer}"  x:Name="ComposerButton" IsReadOnly="True" Foreground="#FFFDFDFD"  />
        <DataGridTextColumn Width="*" Foreground="#FFFDFDFD" Header="TrackName: " Binding="{Binding Name}"  IsReadOnly="True"  x:Name="TrackNameButton"  />
        <DataGridTextColumn Width="*" Foreground="#FFFDFDFD" Header="Price: " Binding="{Binding UnitPrice}"  x:Name="PriceButton"  IsReadOnly="True" />
        <!--<DataGridTextColumn Width="*" Foreground="#FFFDFDFD" Header="artistId: " Binding="{Binding artistId}"  x:Name="artistIdButton"  IsReadOnly="True" />-->
        <DataGridTextColumn Width="*" Foreground="#FFFDFDFD" Header="TrackId: " Binding="{Binding TrackId}"  x:Name="trackId"  IsReadOnly="True" />


        <DataGridTemplateColumn>
          <DataGridTemplateColumn.CellTemplate>
            <DataTemplate>
              <Button x:Name="PlayBtn" Style="{StaticResource default_button}" Click="Play_Stop_Btn_Click">Play</Button>
            </DataTemplate>
          </DataGridTemplateColumn.CellTemplate>
        </DataGridTemplateColumn>
        <DataGridTemplateColumn>

        </DataGridTemplateColumn>
      </DataGrid.Columns>
    </DataGrid>
  </Grid>
</UserControl>